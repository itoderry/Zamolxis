---
name: open-app
description: Open files in a specific desktop app (VS Code, Notepad++, WinMerge, Acrobat, VLC) or diff two files, with the open_app tool.
---
# Open in an app
`open_app` with `app` = vscode | notepad++ | winmerge | acrobat | vlc | default, and `file`. To DIFF two files use app="winmerge" with `file` + `file2`. Use absolute paths. "default" opens the file in its OS-default program.
